export * from './schemas';
import { jsonSchemaFormats } from './formats';
export { formats, formatDefaults } from './formats';
export * from './objects';
export * from '@cypress/schema-tools';
import { PlainObject } from '@cypress/schema-tools';
export declare const api: {
    assertSchema: (name: string, version: string, options?: Partial<{
        greedy: boolean;
        substitutions: string[];
        omit: Partial<{
            errors: boolean;
            object: boolean;
            example: boolean;
        }>;
    }> | undefined) => (object: PlainObject) => PlainObject;
    schemaNames: string[];
    getExample: any;
    sanitize: (name: string, version: string) => (object: PlainObject) => any;
    validate: (schemaName: string, version: string) => (object: object) => true | string[];
    trim: any;
    hasSchema: any;
};
export { jsonSchemaFormats };
